<%= simple_form_for [@list, @bookmark], html: { class: "my-form" } do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.association :movie, label_method: :title, value_method: :id, collection: Movie.order(:title), prompt: 'Film à ajouter', input_html: { class: "form-control" } %>
    <%= f.input :comment, input_html: { class: "form-control" } %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, "Ajouter", class: "btn btn-primary" %>
  </div>
<% end %>
